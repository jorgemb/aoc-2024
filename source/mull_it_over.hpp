#ifndef SOURCE_MULL_IT_OVER
#define SOURCE_MULL_IT_OVER

#include <istream>

class MullItOver{
public:
    static auto solve(std::istream& input) -> int;
    static auto solve_with_logic(std::istream& input) -> int;

private:
};

#endif // SOURCE_MULL_IT_OVER
